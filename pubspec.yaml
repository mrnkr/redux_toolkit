name: redux_toolkit
description: Dart port of the official, opinionated, batteries-included toolset for efficient Redux development
version: 0.0.1

environment:
  sdk: '>=2.0.0-dev.28.0 <3.0.0'

dependencies:
  redux: ^4.0.0+2
  reselect: ^0.4.0
  redux_thunk: ^0.3.0

dev_dependencies:
  test: ^1.3.0
  mockito: ^4.1.1
